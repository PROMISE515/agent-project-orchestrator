# Codex adapter

The bundled `project-company` Skill is explicitly invoked. It assigns one free pod to a new project, creates permanent project memory, and never creates a memory-expiry automation.

Install the agents and Skill as described in the repository root README, then restart Codex if its custom-agent or Skill lists do not refresh.
